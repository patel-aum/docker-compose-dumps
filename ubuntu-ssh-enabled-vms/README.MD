# This compose file has 3 ubuntu vms with ssh enabled with default password 'toor' to test out different scenerios.
- wait for packages to update then you can use following command to excess it locally 'ssh root@localhost -v -p 2222'
